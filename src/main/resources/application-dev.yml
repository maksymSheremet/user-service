spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    baseline-on-migrate: true
logging:
  level:
    my.code.auth: DEBUG
    org.hibernate.SQL: DEBUG